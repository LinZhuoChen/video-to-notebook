---
canonical_name: Parallel Decoding
description: Decoding strategies (Jacobi, lookahead, diffusion-style) that produce
  multiple tokens per step.
ontology_source: discovered
aliases: []
occurrence_count: 18
---
# Parallel Decoding

Decoding strategies (Jacobi, lookahead, diffusion-style) that produce multiple tokens per step.

**18 occurrences** across courses:

- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 3s — The last step which we need to do is the The last step which we need to do is the
actual step of dnoising. actual step o
- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 51s — yeah, at each step of the training yeah, at each step of the training
process, if we are able to predict how process, if
- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 95s — recover the clean image. So we start
from a complete noisy image and then we from a complete noisy image and then we fro
- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 135s — parameters of the model will not change
in the dnoising process. The model is in the dnoising process. The model is in t
- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 182s — time steps, for example, in the D
noising process, we have to go backwards noising process, we have to go backwards nois
- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 229s — let's take an example. Okay. So if this let's take an example. Okay. So if this
is the input sequence we start at time i
- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 282s — What is the predicted logits matrix?
Um so let's say we have position number Um so let's say we have position number Um 
- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 358s — then we have a b and c right so let's then we have a b and c right so let's
actually actually actually
take some differe
- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 436s — change this again.
Sorry for that. So let's say this 336 Sorry for that. So let's say this 336 Sorry for that. So let's 
- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 487s — we need three masks here. So again we'll
mask all these three. So these three mask all these three. So these three mask 
- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 532s — of this step we have to keep three masks
at the end of this step we have to keep at the end of this step we have to keep
- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 571s — or I will keep so which what what will I or I will keep so which what what will I
unmask? So I have to keep two masks he
- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 609s — confidence of 3175. At this we have a
confidence of 336 and at this we have a confidence of 336 and at this we have a co
- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 654s — So at first position I sample A with
confidence of 3175. At second position I confidence of 3175. At second position I c
- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 705s — input architecture and wherever there is input architecture and wherever there is
mask that slowly unmasked right. So he
- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 752s — reggressive which means it's not one
token at a time. Tokens masks can be token at a time. Tokens masks can be token at 
- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 797s — what happens is that let's say you fully
trained the model as we have seen trained the model as we have seen trained the
- **diffusion-lm-vizuara** L15 (Lecture 14: Diffusion LLM Inference Pipeline) @ 846s — unmasking one after the other. So this unmasking one after the other. So this
video or this GIF which Andre Karpati vide
