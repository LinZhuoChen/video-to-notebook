---
canonical_name: AR vs Diffusion
description: Comparison axis — autoregressive sequential decoding vs parallel iterative
  refinement; throughput, quality, and infilling trade-offs.
ontology_source: discovered
aliases: []
occurrence_count: 114
---
# AR vs Diffusion

Comparison axis — autoregressive sequential decoding vs parallel iterative refinement; throughput, quality, and infilling trade-offs.

**114 occurrences** across courses:

- **diffusion-lm-vizuara** L2 (Lecture 1: Introduction to Diffusion Language Models) @ 3s — Hello everyone. This is Dr. Raj Dandeek Hello everyone. This is Dr. Raj Dandeek
and uh today I'm very excited to present
- **diffusion-lm-vizuara** L2 (Lecture 1: Introduction to Diffusion Language Models) @ 61s — models from scratch. But that's not how models from scratch. But that's not how
I want this course or this learning I wa
- **diffusion-lm-vizuara** L2 (Lecture 1: Introduction to Diffusion Language Models) @ 109s — scared of them, then how I learned the scared of them, then how I learned the
subject, how I mastered the subject now su
- **diffusion-lm-vizuara** L2 (Lecture 1: Introduction to Diffusion Language Models) @ 147s — model fully from scratch. You'll compare
it with the auto reggressive language it with the auto reggressive language it 
- **diffusion-lm-vizuara** L6 (Lecture 5: Motivation behind Language Diffusion Models) @ 2s — So I hope all of us are on the same page So I hope all of us are on the same page
now with respect to the motivation now
- **diffusion-lm-vizuara** L6 (Lecture 5: Motivation behind Language Diffusion Models) @ 50s — questions which researchers should ask
that leads to the best research. that leads to the best research. that leads to t
- **diffusion-lm-vizuara** L6 (Lecture 5: Motivation behind Language Diffusion Models) @ 109s — models. This so this paper I believe
is a very important one mask diffusion is a very important one mask diffusion is a 
- **diffusion-lm-vizuara** L6 (Lecture 5: Motivation behind Language Diffusion Models) @ 171s — introduction section itself
uh that why should images and video uh that why should images and video uh that why should i
- **diffusion-lm-vizuara** L6 (Lecture 5: Motivation behind Language Diffusion Models) @ 219s — images might lie and then sampling from
it. So why can't this same thing be it. So why can't this same thing be it. So w
- **diffusion-lm-vizuara** L6 (Lecture 5: Motivation behind Language Diffusion Models) @ 298s — when we talked about images, first we
broke down the images into pixels, broke down the images into pixels, broke down t
- **diffusion-lm-vizuara** L6 (Lecture 5: Motivation behind Language Diffusion Models) @ 354s — So one way to look at generating text is So one way to look at generating text is
just one word at a time. Another way o
- **diffusion-lm-vizuara** L6 (Lecture 5: Motivation behind Language Diffusion Models) @ 417s — in high dimensional spaces? What does it
mean probability distribution of mean probability distribution of mean probabil
- **diffusion-lm-vizuara** L6 (Lecture 5: Motivation behind Language Diffusion Models) @ 492s — sequence of words based on two things. sequence of words based on two things.
Grammar and logic both need to be Grammar 
- **diffusion-lm-vizuara** L6 (Lecture 5: Motivation behind Language Diffusion Models) @ 552s — can be carried forward to languages. can be carried forward to languages.
What these people suggested is auto What these
- **diffusion-lm-vizuara** L7 (Lecture 6: Auto Regressive Models (ARMs) Introduction) @ 3s — To understand the concept of auto To understand the concept of auto
reggressive models, reggressive models, reggressive 
- **diffusion-lm-vizuara** L7 (Lecture 6: Auto Regressive Models (ARMs) Introduction) @ 97s — upon a time and I want the language
model to complete this prompt. model to complete this prompt. model to complete this
- **diffusion-lm-vizuara** L7 (Lecture 6: Auto Regressive Models (ARMs) Introduction) @ 143s — sequence then the next token is
predicted this is during the training predicted this is during the training predicted th
- **diffusion-lm-vizuara** L7 (Lecture 6: Auto Regressive Models (ARMs) Introduction) @ 186s — see multiple tokens here, it's one token
being generated at a time and the next being generated at a time and the next b
- **diffusion-lm-vizuara** L7 (Lecture 6: Auto Regressive Models (ARMs) Introduction) @ 238s — one thing which works. But why can't
diffusion be the default option? This is diffusion be the default option? This is d
- **diffusion-lm-vizuara** L7 (Lecture 6: Auto Regressive Models (ARMs) Introduction) @ 286s — predicts one token at a time. So if you
see to generate the same amount of text see to generate the same amount of text 
- **diffusion-lm-vizuara** L7 (Lecture 6: Auto Regressive Models (ARMs) Introduction) @ 334s — it's fine because it's one new token
generated at a time but this reverse generated at a time but this reverse generated
- **diffusion-lm-vizuara** L7 (Lecture 6: Auto Regressive Models (ARMs) Introduction) @ 381s — mentioned is that if the true language
distribution lies somewhere here can we distribution lies somewhere here can we d
- **diffusion-lm-vizuara** L7 (Lecture 6: Auto Regressive Models (ARMs) Introduction) @ 433s — what's the need of auto reggressive what's the need of auto reggressive
models to achieve this to make the models to ach
- **diffusion-lm-vizuara** L7 (Lecture 6: Auto Regressive Models (ARMs) Introduction) @ 482s — ure of diffusion models differs from ure of diffusion models differs from
auto reggressive models and then we are auto r
- **diffusion-lm-vizuara** L8 (Lecture 7: Auto Regressive Models (ARM) Architecture Intro) @ 2s — To truly understand To truly understand
diffusion language models, we actually diffusion language models, we actually di
- **diffusion-lm-vizuara** L8 (Lecture 7: Auto Regressive Models (ARM) Architecture Intro) @ 67s — small aspect like the feed forward small aspect like the feed forward
neural network block here etc. So this neural netw
- **diffusion-lm-vizuara** L8 (Lecture 7: Auto Regressive Models (ARM) Architecture Intro) @ 125s — model. This data set essentially
consists of around 2 million stories consists of around 2 million stories consists of a
- **diffusion-lm-vizuara** L8 (Lecture 7: Auto Regressive Models (ARM) Architecture Intro) @ 171s — so that I can predict or I can generate
language. language. language.
And what does what does it mean? So the And what d
- **diffusion-lm-vizuara** L8 (Lecture 7: Auto Regressive Models (ARM) Architecture Intro) @ 238s — want to do. And what does it mean learn
patterns from the data? Well, the model patterns from the data? Well, the model 
- **diffusion-lm-vizuara** L8 (Lecture 7: Auto Regressive Models (ARM) Architecture Intro) @ 286s — the kicked. That means it has not
understand not understood the correct understand not understood the correct understand
- **diffusion-lm-vizuara** L8 (Lecture 7: Auto Regressive Models (ARM) Architecture Intro) @ 340s — Yeah. So
how do we get started with this task? how do we get started with this task? how do we get started with this tas
- **diffusion-lm-vizuara** L8 (Lecture 7: Auto Regressive Models (ARM) Architecture Intro) @ 409s — size. It defines how many tokens or how size. It defines how many tokens or how
many words the model can pay attention m
- **diffusion-lm-vizuara** L8 (Lecture 7: Auto Regressive Models (ARM) Architecture Intro) @ 458s — if this is my first input sequence,
right? I shift it to the right hand side right? I shift it to the right hand side ri
- **diffusion-lm-vizuara** L8 (Lecture 7: Auto Regressive Models (ARM) Architecture Intro) @ 518s — model to learn that given any token it model to learn that given any token it
has to predict the next token. So if one h
- **diffusion-lm-vizuara** L8 (Lecture 7: Auto Regressive Models (ARM) Architecture Intro) @ 564s — input, little will be the output. And if
one day a little is the input, girl will one day a little is the input, girl wi
- **diffusion-lm-vizuara** L8 (Lecture 7: Auto Regressive Models (ARM) Architecture Intro) @ 624s — so imagine the huge data set which we
had 2 million rows that's but that's had 2 million rows that's but that's had 2 mi
- **diffusion-lm-vizuara** L8 (Lecture 7: Auto Regressive Models (ARM) Architecture Intro) @ 679s — and the true answer, I have my loss
function. Right? Once I have my loss function. Right? Once I have my loss function. 
- **diffusion-lm-vizuara** L8 (Lecture 7: Auto Regressive Models (ARM) Architecture Intro) @ 730s — Um, then what I'll do is that I go
through my second batch, I get the loss, through my second batch, I get the loss, thr
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 2s — So the way a batch is constructed is as So the way a batch is constructed is as
follows. Let's say for now we just follo
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 50s — the third stage is the output stage.
When we look at diffusion language When we look at diffusion language When we look 
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 89s — first is that since the model cannot
understand text these tokens these are understand text these tokens these are under
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 147s — is given a token ID of 1 then this is 12 is given a token ID of 1 then this is 12
then this is 50 and this is 80. then t
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 192s — model is not trained the vectors for cat
and kitten might lie further apart from and kitten might lie further apart from
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 241s — This is my whole vocabulary set. I
assign token ids to each of them. Let's assign token ids to each of them. Let's assig
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 295s — If I want to retrieve the embedding
vectors for these, I just go to my token vectors for these, I just go to my token ve
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 352s — the same words. So their token embedding
vectors will be the same but they come vectors will be the same but they come v
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 405s — length. So this goes from one to right
up to the sequence length. And I have a up to the sequence length. And I have a u
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 464s — corresponding to position one this is
the vector vector corresponding to the vector vector corresponding to the vector v
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 508s — block is where all the magic happens. So
initially we started out by uh thinking initially we started out by uh thinking
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 553s — are also calling processor here. are also calling processor here.
This transformer block consists of all This transforme
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 598s — by another layer normalization followed
by feed forward followed by dropout. The by feed forward followed by dropout. Th
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 646s — that so now the input tokens let's say
so ideally we should we should have so ideally we should we should have so ideall
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 688s — root of variance. Why do we do this? root of variance. Why do we do this?
Because normalization really helps the Because
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 737s — normalization. The second block which I normalization. The second block which I
want to emphasize a lot over here. This 
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 791s — Harry Potter daughter
or Harry lived or Harry lived or Harry lived
at Hogwarts and he so let's say this is my input and 
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 845s — have not linked one token to another have not linked one token to another
token anywhere. Even this uh layer token anywh
- **diffusion-lm-vizuara** L9 (Lecture 8: Auto Regressive Models (ARM) Architecture Data-Transformer Blocks) @ 898s — the mask diffusion models as well. So
let's understand this attention let's understand this attention let's understand t
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 3s — So in attention as I mentioned the main So in attention as I mentioned the main
idea is to have these token embeddings i
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 52s — to capture
if I'm looking at a particular token if I'm looking at a particular token if I'm looking at a particular toke
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 110s — vectors, value vectors, etc. So what is vectors, value vectors, etc. So what is
done is that we have this input matrix, 
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 153s — with query key and values in the
attention block. Right? attention block. Right? attention block. Right?
Once we have th
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 196s — by 5 right 1 2 3 4 5 and let me
duplicate this duplicate this duplicate this
1 2 3 1 2 3 1 2 3
4 and 5. 4 and 5. 4 and 5
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 259s — statements like if I'm looking at
bright, bright, bright,
I want to make statements like give 10% I want to make stateme
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 310s — to or give 50% importance to bright.
Um there is also the scaling by square Um there is also the scaling by square Um th
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 348s — multiply it with the value matrix and
this gives me my context vector matrix. this gives me my context vector matrix. th
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 405s — So what I'll do is that I'll take the So what I'll do is that I'll take the
values vector for each of these and values v
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 457s — tokens. But now what I'm saying is that
when we look at next I have information when we look at next I have information 
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 510s — Hogwarts and he Hogwarts and he
now I have encoded how he relates to now I have encoded how he relates to now I have enc
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 570s — So, we are doing the next token
prediction task here. Um, so if next is prediction task here. Um, so if next is predicti
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 611s — is no need of finding the attention
score between is and bright. But for score between is and bright. But for score betw
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 659s — essentially, I only need to find the
attention scores of what comes in this attention scores of what comes in this atten
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 702s — these attention score values are these attention score values are
essentially set to zero because we don't essentially s
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 748s — attention mass does not exist for attention mass does not exist for
diffusion models. In multi head diffusion models. In
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 799s — that causal attention mask here right we
don't need these attention score so we don't need these attention score so we d
- **diffusion-lm-vizuara** L10 (Lecture 9: Auto Regressive Models (ARM) Attention Block) @ 841s — explanation which I've tried to give explanation which I've tried to give
you. If you want to go and drill down you. If 
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 3s — So once we come out of the attention So once we come out of the attention
block, we have a dropout layer. Uh let block, 
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 55s — these vectors to zero. And the reason
dropout is implemented is to improve the dropout is implemented is to improve the 
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 97s — the generalization performance, right?
U so after dropout we again have these U so after dropout we again have these U s
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 146s — we want to avoid the problem of
vanishing gradients. vanishing gradients. vanishing gradients.
The thing is if multiple 
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 203s — So we have to go from here to now here So we have to go from here to now here
which is the second layer normalization. w
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 248s — The activation layer here which is
generally preferred is JLU activation. generally preferred is JLU activation. general
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 298s — been borrowed and which show up
repeatedly. So which are these elements? repeatedly. So which are these elements? repeat
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 344s — output to the transformer block. The
output of the transformer block is this. output of the transformer block is this. o
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 415s — build neural networks from scratch, I build neural networks from scratch, I
think. Let's see. Or build LLM from think. L
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 504s — block goes to the second the output of
the second goes to the third. The output the second goes to the third. The output
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 545s — say transformer block output it's the say transformer block output it's the
output from those 12 24 or 96 output from th
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 604s — moves you right. This has as we have
seen this is one input sequence but it seen this is one input sequence but it seen 
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 661s — training. Right? Initially I would have
some random random guesses. So this is some random random guesses. So this is so
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 707s — and the predictions. Okay. So, how do we
get to the predictions? Currently we get to the predictions? Currently we get t
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 754s — that's the vocabulary size which will be that's the vocabulary size which will be
the number of columns. the number of c
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 821s — vector it will make sure that each value vector it will make sure that each value
lies between 0 to one and they sum up 
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 876s — maybe token ID number 40,000
which is a boy So these are my predictions now and then So these are my predictions now and
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 934s — each other. As the target and the
prediction start to approximate each prediction start to approximate each prediction s
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 987s — model is trained properly the model is trained properly the
probability at token ID number three probability at token ID
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 1049s — my loss will be zero which is what we
want. But in this case the loss will be want. But in this case the loss will be wa
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 1173s — probability which should actually be one
but maybe it's.3 if every effort moves but maybe it's.3 if every effort moves b
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 1286s — if we take negative of this it will be if we take negative of this it will be
equal to 2.619 619 that's the loss for equ
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 1334s — models are trained. I hope all of you
now see the end to end details of why now see the end to end details of why now se
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 1375s — All right. So if you take a look at the
parameters now I hope all of you can parameters now I hope all of you can parame
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 1430s — is the positional embeddings also a is the positional embeddings also a
parameter? Because we have the token parameter? 
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 1489s — attention mechanism. So that's also a
parameter. If you scroll about to the parameter. If you scroll about to the parame
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 1556s — this is the feed forward neural network.
So we have 768 dimensions going to 4 So we have 768 dimensions going to 4 So we
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 1604s — parameters. We have parameters in the
final layer norm. And in the output final layer norm. And in the output final laye
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 1666s — contributes to the parameters in the
output layer. So all of these parameters output layer. So all of these parameters o
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 1711s — start of this that to truly understand start of this that to truly understand
diffusion language models, we really diffu
- **diffusion-lm-vizuara** L11 (Lecture 10: Auto Regressive Models (ARM) Output and Loss Blocks) @ 1760s — we'll see how the ARM architecture is
the backbone of this predicting noise the backbone of this predicting noise the ba
- **diffusion-lm-vizuara** L12 (Lecture 11: Auto Regressive Models (ARM) Code: Pre training and Inference) @ 2s — Before moving on to the fusion model Before moving on to the fusion model
architecture architecture architecture
and uh 
- **diffusion-lm-vizuara** L12 (Lecture 11: Auto Regressive Models (ARM) Code: Pre training and Inference) @ 59s — continues. That's pre-training of continues. That's pre-training of
language models or traditional language language mod
- **diffusion-lm-vizuara** L12 (Lecture 11: Auto Regressive Models (ARM) Code: Pre training and Inference) @ 110s — we get the token embeddings we get the we get the token embeddings we get the
position embeddings we add the token posit
- **diffusion-lm-vizuara** L12 (Lecture 11: Auto Regressive Models (ARM) Code: Pre training and Inference) @ 160s — function like the way we have um looked
at on the whiteboard at on the whiteboard at on the whiteboard
right we saw that
- **diffusion-lm-vizuara** L12 (Lecture 11: Auto Regressive Models (ARM) Code: Pre training and Inference) @ 204s — essentially the context length or the
sequence length. That's the number of sequence length. That's the number of sequen
- **diffusion-lm-vizuara** L12 (Lecture 11: Auto Regressive Models (ARM) Code: Pre training and Inference) @ 257s — not. Right? The whole idea when we
started this exercise was started this exercise was started this exercise was
um we w
- **diffusion-lm-vizuara** L12 (Lecture 11: Auto Regressive Models (ARM) Code: Pre training and Inference) @ 298s — completely correct here. There are some
mistakes here and there, but overall the mistakes here and there, but overall th
- **diffusion-lm-vizuara** L12 (Lecture 11: Auto Regressive Models (ARM) Code: Pre training and Inference) @ 340s — exactly, but mostly it's pretty cool. So
auto reggressive models definitely work auto reggressive models definitely work
